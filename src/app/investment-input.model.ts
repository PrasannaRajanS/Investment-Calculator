export interface InvestmentInput { // instead of using an interface we can use type it is upto our wish
    initialInvestment: number, 
    duration: number, 
    expectedReturn: number, 
    annualInvestment: number
}